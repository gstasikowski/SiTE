<Page x:Class="SiTE.Views.SaveReminderView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:SiTE.Views"
      mc:Ignorable="d" 
      d:DesignHeight="200" d:DesignWidth="800"
      Title="SaveReminderView">

    <Grid>
        <StackPanel Orientation="Vertical">
            <Label x:Name="lbl_MessageTitle" Content="Message_Title" HorizontalAlignment="Center"/>
            <Label x:Name="lbl_MessageBody" Content="{DynamicResource MessageSaveReminder}"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Bottom">
            <Button x:Name="btn_SaveYes" Content="{DynamicResource ButtonYes}" Margin="2"/>
            <Button x:Name="btn_SaveNo" Content="{DynamicResource ButtonNo}" Margin="2"/>
            <Button x:Name="btn_SaveCancel" Content="{DynamicResource ButtonCancel}" Margin="2"/>
        </StackPanel>
    </Grid>
</Page>
