<Window x:Class="SiTE.Views.SaveReminderView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:SiTE.Views"
      mc:Ignorable="d" 
      d:DesignHeight="200" d:DesignWidth="800"
      Title="{DynamicResource WindowSaveReminder}" ResizeMode="NoResize" SizeToContent="WidthAndHeight" WindowStartupLocation="CenterOwner">

    <Grid>
        <StackPanel>
            <Label Content="{DynamicResource MessageSaveReminder}" Padding="15,5,15,15"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,5">
                <Button Content="{DynamicResource ButtonYes}" Padding="3" Margin="2" Click="BtnSave_Click"/>
                <Button Content="{DynamicResource ButtonNo}" Padding="3"  Margin="2" Click="BtnProceed_Click"/>
                <Button Content="{DynamicResource ButtonCancel}" Padding="3"  Margin="2" Click="BtnCancel_Click"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
